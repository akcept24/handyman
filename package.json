{
  "name": "california-handyman-site",
  "version": "1.0.0",
  "private": true,
  "description": "California Handyman website and lead delivery service",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/server.test.js && python3 -m unittest -v tests/test_site.py"
  },
  "engines": {
    "node": ">=20"
  }
}
